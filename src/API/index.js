//Write a function to fetch all the players from the API
//Then, import into AllPlayers.jsx component

export async function fetchPlayers() {
    try {
        const response = await fetch('');
        const data = await response.json();

    }
}


//Write a funtion to fetch single player in API and render (just like above)